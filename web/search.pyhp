<html>
<body>
<ol>
<?

query_string = args["q"]
query = lambda x: x["name"].find(query_string)>0 or x["desc"].find(query_string)>0

print("test")
import metatorrent
MT = metatorrent.init()
for result in MT.search(query):
    print(result)
    write("<li>%s | %s</li>"%(result["name"],result["desc"]))


?>
</ol>
</body>
</html>
